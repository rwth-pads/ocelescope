import { RouteDefinition } from "@/plugins/types";

const ObjectTypes = () => {
  return <>ObjectTypes</>;
};

export default ObjectTypes;

export const config: RouteDefinition = { name: "Object Types" };
