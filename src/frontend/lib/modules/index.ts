import type { ModuleDefinition, ModuleRouteDefinition } from "@/types/modules";

export const defineModule = (def: ModuleDefinition) => def;

export const defineModuleRoute = (def: ModuleRouteDefinition) => def;
